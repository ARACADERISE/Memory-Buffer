#ifdef BUFFER_
#define BUFFER_ENGINE

typedef struct BUFFER {

} buffer;

#endif
/* We should be all setup by this point */
#ifdef BUFFER_ENGINE
#define BUFFER_ENGINE_

typedef struct BUFFER_ {

} buffer_;

#endif